<%= form_for(@product_listing) do |f| %>
  <% if @product_listing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_listing.errors.count, "error") %> prohibited this product_listing from being saved:</h2>

      <ul>
      <% @product_listing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
